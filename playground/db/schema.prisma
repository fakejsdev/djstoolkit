datasource db {
    provider = "postgresql"
}

generator client {
    provider = "prisma-client"
    output   = "../src/lib/prisma/generated"
}

model User {
    id    String @id
    xp    Int    @default(0)
    level Int    @default(1)
}
