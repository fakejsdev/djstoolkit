datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "../src/lib/prisma/generated"
}

model User {
  id   Int    @id @default(autoincrement())
  name String @unique
}
